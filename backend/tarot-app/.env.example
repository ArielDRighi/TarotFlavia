# =============================================================================
# TAROTFLAVIA - DOCKER ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains all environment variables needed for Docker services.
# Copy this file to .env and update the values according to your setup.
#
# IMPORTANT: Never commit your .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Database Configuration
# -----------------------------------------------------------------------------
# Database connection settings for TarotFlavia PostgreSQL container

# Port exposed on your host machine (avoid conflicts with other PostgreSQL instances)
# Default: 5435 (other projects use 5432, 5433, 5434)
TAROTFLAVIA_DB_PORT=5435

# PostgreSQL superuser credentials
TAROTFLAVIA_DB_USER=tarotflavia_user
TAROTFLAVIA_DB_PASSWORD=tarotflavia_secure_password_2024

# Database name
TAROTFLAVIA_DB_NAME=tarotflavia_db

# PostgreSQL version (referenced in docker-compose.yml)
TAROTFLAVIA_POSTGRES_VERSION=16-alpine

# -----------------------------------------------------------------------------
# pgAdmin Configuration (Optional)
# -----------------------------------------------------------------------------
# Web interface for PostgreSQL database management
# Access at: http://localhost:5050 (or your configured port)

# Port exposed on your host machine for pgAdmin web interface
TAROTFLAVIA_PGADMIN_PORT=5050

# pgAdmin login credentials
TAROTFLAVIA_PGADMIN_EMAIL=admin@tarotflavia.local
TAROTFLAVIA_PGADMIN_PASSWORD=admin123

# -----------------------------------------------------------------------------
# NestJS Backend Configuration (for connecting to Docker PostgreSQL)
# -----------------------------------------------------------------------------
# These variables should be used in your backend .env file

# Database connection string
TAROTFLAVIA_DATABASE_URL=postgresql://tarotflavia_user:tarotflavia_secure_password_2024@localhost:5435/tarotflavia_db

# Individual database connection parameters (for TypeORM)
TAROTFLAVIA_DB_HOST=localhost
TAROTFLAVIA_DB_PORT_INTERNAL=5435
TAROTFLAVIA_DB_USERNAME=tarotflavia_user
TAROTFLAVIA_DB_PASSWORD_VALUE=tarotflavia_secure_password_2024
TAROTFLAVIA_DB_DATABASE=tarotflavia_db

# TypeORM specific settings
TAROTFLAVIA_DB_SYNCHRONIZE=true
TAROTFLAVIA_DB_LOGGING=true

# -----------------------------------------------------------------------------
# Development vs Production
# -----------------------------------------------------------------------------
# NODE_ENV=development  # Use this for local development with Docker
# NODE_ENV=production   # Use this for production deployments

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# 1. Update passwords with secure values before deploying to production
# 2. Port 5435 is used to avoid conflicts with:
#    - Port 5432: ecommerce-postgres
#    - Port 5433: microservices-postgres
#    - Port 5434: go-api-postgres
# 3. To start only PostgreSQL: docker-compose up -d tarotflavia-postgres
# 4. To start with pgAdmin: docker-compose --profile tools up -d
# 5. All container names, volumes, and networks have 'tarotflavia' prefix
#    to avoid confusion with other Docker projects
